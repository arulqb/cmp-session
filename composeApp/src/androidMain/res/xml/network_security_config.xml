<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.29.25</domain>
        <!-- You can add other local IPs or specific domains here -->
        <!-- <domain includeSubdomains="true">localhost</domain> -->
    </domain-config>
    <!-- You can also set a base-config to allow cleartext for all domains (NOT recommended for production) -->
    <!--
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
     -->
</network-security-config>